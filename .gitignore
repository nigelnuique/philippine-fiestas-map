node_modules/
dist/
.env
.env.*
!.env.example
!.env.production

# Pipeline scratch / internal state (keep out of git when possible)
data/processed/festivals/harvest-logs/
data/processed/festivals/official-barangay-fiesta-*.json
*.log
.DS_Store
Thumbs.db

# Raw downloads (large); processed outputs are tracked
data/raw/
data/cache/

# Synced static assets for the map app (regenerated via npm run map:sync)
public/geojson/
public/data/

# OS / editor
.idea/
.vscode/
.cursor/
*.swp

# Playwright
playwright-report/
test-results/

# Data-pipeline scratch (keep scripts/ production files tracked)
scripts/tmp-*
scripts/tmp/
